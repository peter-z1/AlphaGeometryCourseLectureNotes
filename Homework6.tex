\documentclass{article}
\usepackage{graphicx} % Required for inserting images
\newtheorem{problem}{Problem}
\usepackage{amssymb}
\usepackage{amsfonts}
\usepackage{amsmath}

\newcommand{\parallelsum}{\mathbin{\!/\mkern-5mu/\!}}




\title{Math1810c Homework 6}
\author{Released October 17}
\date{Due October 24}

\begin{document}

\maketitle

\section{Instructions}

For the homework, you must write your own solutions for the Mathematical Problems. For the coding problems, you will work in your teams, and should have a commit on your Github page for the tasks for each week. 

\section{Mathematical Problems}

\begin{problem}
    Let $ABCD$ be a quadrilateral whose sides $AB$, $BC$, $CD$, $DA$ are tangent to a single circle at points $M$, $N$, $P$, $Q$, respectively. Prove that the lines $AC$, $BD$, $MP$, $NQ$ are concurrent. 
\end{problem}

%\begin{problem}
%    With notation as in the previous problem let lines $BQ$, $BP$ intersect the circle at $E$ and $F$ respectively. Prove that $ME$, $NF$, and $BD$ are concurrent. 
%\end{problem}

\begin{problem}
    Let $n\geq 4$. Let $S=[n]$ denote the set $\{1,2,3,...,n\}$ denote a set of ``points'', do the following families of subsets of $S$ (i.e. ``lines'') satisfy Hilbert's incidence axioms for geometry. 
    \begin{enumerate}
        \item $\binom{[n]}{2}=\{X\subseteq [n]: \vert X\vert =2\}$ (i.e. the set of all two element subsets of $[n]$.)
        \item $\binom{[n]}{n-1}=\{X\subseteq [n]: \vert X\vert =n-1\}$ (i.e. the set of all $n-1$ element subsets of $[n]$.)
        \item Here the lines consist of sets of the form $\{1,k\}$ where $2\leq k\leq n$ and the set $\{2,3,4,...,n\}$.
    \end{enumerate}
\end{problem}

\newpage

\section{Coding Goals}

\begin{problem}
    Next week (October 22 \& 24) in class, we will be testing our systems on the problems from homework 4 that your team did not receive, and perhaps some of the problems that other groups came up with for homework 5. Prepare your system for this first initial ``test'' of its ability. 
\end{problem}

\end{document}
